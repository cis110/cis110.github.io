public class ShapeDemo {
    public static void main(String[] args) {
        // Shape c = new Circle(0.75, 0.75, 0.25);
        // Shape s = new Square(0.25, 0.25, 0.25);
        // Shape[] shapes = new Shape[2];
        // shapes[0] = c;
        // shapes[1] = s;

        // for (int i = 0; i < shapes.length; i++) {
        // shapes[i].draw();
        // }
    }
}